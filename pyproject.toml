[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mmai-synth"
version = "0.1.0"
description = "MMAI synthetic data pipeline"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
mmai = "mmai.cli:app"
